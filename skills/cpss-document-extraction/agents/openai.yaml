interface:
  display_name: "CPSS Document Extraction"
  short_description: Extract and validate data from documents.
  default_prompt: Use $cpss-document-extraction to extract and validate structured information
    from screenshots, PDFs, spreadsheets, pasted tables, or typed documents before
    downstream analysis.
policy:
  products:
  - CHAT
  - CODEX
  allow_implicit_invocation: true
